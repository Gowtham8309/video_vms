# Core Frameworks
streamlit
gradio>=5.0.0
dashscope
tqdm

# Transformers & Diffusion
transformers>=4.49.0
tokenizers>=0.20.3
diffusers>=0.31.0
accelerate>=1.1.1
sentencepiece  # Required for Mistral tokenizer (manually install if needed)

# PyTorch (CUDA 12.1 specific build)
--index-url https://download.pytorch.org/whl/cu121
torch>=2.4.0
torchvision>=0.19.0
bitsandbytes  # Quantization (Mistral, GGUF, etc.)

# Vision + Media Processing
opencv-python>=4.9.0.80
Pillow
imageio
imageio-ffmpeg
einops==0.7
numpy>=1.23.5,<2
librosa

# Text & Audio
ftfy
gTTS  # For voice-over (optional)

# Utilities
easydict

# NOTE: Install flash-attn manually as it's environment-specific:
# pip install flash-attn --no-build-isolation --force-reinstall
